# Makes the `engines` field in package.json a hard error rather than a warning,
# so `npm install` fails fast on an unsupported Node version instead of letting
# the mismatch surface later as a confusing jest or next crash.
engine-strict=true
